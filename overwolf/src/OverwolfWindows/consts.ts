const newWorldId = 21816;

const interestingFeatures = [
    'location',
];

const windowNames = {
    inGame: 'in_game',
    desktop: 'desktop',
} as const;

const hotkeys = {
    toggle: 'showhide',
} as const;

export {
    newWorldId,
    interestingFeatures,
    windowNames,
    hotkeys,
};
