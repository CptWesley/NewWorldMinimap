<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <appSettings>
    <add key="updateFrequency" value="500"/> <!--the delay between checking for position updates, in milliseconds. if it is 0 you need to press 'r' on the window to force a position update -->
  </appSettings>
</configuration>